````markdown
# ✨ 新功能：语言和模型选择

## 已添加的功能

### 🌍 语言选择
在创建事件时，现在可以选择字幕生成的语言：

**支持的语言**：
- **Auto Detect** - 自动检测（推荐）
- English - 英语
- 中文 (Chinese) - 中文
- Español (Spanish) - 西班牙语
- Français (French) - 法语
- Deutsch (German) - 德语
- 日本語 (Japanese) - 日语
- 한국어 (Korean) - 韩语
- Português (Portuguese) - 葡萄牙语
- Русский (Russian) - 俄语
- العربية (Arabic) - 阿拉伯语
- हिन्दी (Hindi) - 印地语

### 🤖 Whisper模型选择
根据你的需求选择不同的模型：

| 模型 | 速度 | 质量 | 推荐场景 |
|------|------|------|----------|
| **Tiny** | ⚡⚡⚡⚡⚡ | ⭐⭐ | 快速测试 |
| **Base** | ⚡⚡⚡⚡ | ⭐⭐⭐ | 日常使用（推荐）|
| **Small** | ⚡⚡⚡ | ⭐⭐⭐⭐ | 更好质量 |
| **Medium** | ⚡⚡ | ⭐⭐⭐⭐⭐ | 高质量需求 |
| **Large V3** | ⚡ | ⭐⭐⭐⭐⭐⭐ | 最佳质量 |

**处理时间对比**（30分钟视频）：
- Tiny: ~1分钟
- Base: ~3分钟
- Small: ~6分钟
- Medium: ~12分钟
- Large V3: ~25分钟

---

## 如何使用

### 步骤 1: 重启服务器

**前端**（如果需要）：
```bash
cd frontend
npm run dev
```

**后端**：
```bash
# 停止旧服务器 (Ctrl+C)
python api_server.py
```

### 步骤 2: 创建事件

1. 访问 http://localhost:3000/events
2. 点击 "New Event"
3. 填写事件信息
4. 在 **Language** 下拉框选择语言（或使用 Auto Detect）
5. 在 **Whisper Model** 下拉框选择模型
6. 勾选 "Generate Subtitles"
7. 点击 "Create Event"

### 步骤 3: 上传视频并运行

1. 在事件详情页点击 "Upload Video"
2. 输入视频路径
3. 点击 "Run Workflow"
4. 等待完成

---

## 使用建议

### 场景 1: 英语证道（快速处理）
- **Language**: English
- **Model**: Base
- **预期时间**: 30分钟视频 → 3分钟

### 场景 2: 中文证道（高质量）
- **Language**: 中文
- **Model**: Small 或 Medium
- **预期时间**: 30分钟视频 → 6-12分钟

### 场景 3: 混合语言（自动检测）
- **Language**: Auto Detect
- **Model**: Base 或 Small
- **预期时间**: 30分钟视频 → 3-6分钟

### 场景 4: 最高质量（不赶时间）
- **Language**: 按需选择
- **Model**: Large V3
- **预期时间**: 30分钟视频 → 25分钟

---

## 下载模型

如果你选择的模型还没有下载，系统会提示你。

### 手动下载模型

```bash
cd ../whisper.cpp/models

# 下载特定模型
./download-ggml-model.sh tiny
./download-ggml-model.sh base
./download-ggml-model.sh small
./download-ggml-model.sh medium
./download-ggml-model.sh large-v3

# 查看已下载的模型
ls -lh *.bin
```

### 可用模型位置
```
../whisper.cpp/models/
├── ggml-tiny.bin       (~75 MB)
├── ggml-base.bin       (~142 MB) ✓ 已有
├── ggml-small.bin      (~466 MB)
├── ggml-medium.bin     (~1.5 GB)
└── ggml-large-v3.bin   (~3.1 GB)
```

---

## 事件配置示例

创建事件后，配置文件会包含：

```json
{
  "event_id": "2026-01-27_1200_sunday-sermon",
  "title": "Sunday Sermon",
  "speaker": "Pastor John",
  "language": "en",
  "whisper_model": "small",
  "modules": {
    "subtitles": true
  }
}
```

运行工作流时，日志会显示：

```
INFO - Running subtitle generation...
INFO - Using Whisper model: small, Language: en
INFO - Subtitles generated: {'srt': '...', 'vtt': '...'}
```

---

## 语言代码参考

如果需要添加更多语言，使用ISO 639-1代码：

| 代码 | 语言 |
|------|------|
| en | English |
| zh | Chinese |
| es | Spanish |
| fr | French |
| de | German |
| ja | Japanese |
| ko | Korean |
| pt | Portuguese |
| ru | Russian |
| ar | Arabic |
| hi | Hindi |
| it | Italian |
| nl | Dutch |
| pl | Polish |
| tr | Turkish |
| vi | Vietnamese |
| th | Thai |

---

## 故障排除

### 问题: 选择的模型不可用

**错误信息**:
```
Model 'small' not found. Please download it first.
```

**解决方法**:
```bash
cd ../whisper.cpp/models
./download-ggml-model.sh small
```

### 问题: 语言选择不生效

**检查**:
- 确保重启了API服务器
- 查看事件的 `event.json` 文件确认语言已保存
- 查看服务器日志确认使用了正确的语言

### 问题: 自动检测语言不准确

**解决方法**:
- 手动选择正确的语言
- 使用更大的模型（如 Small 或 Medium）
- 确保音频清晰度足够

---

## 性能优化建议

### 快速迭代测试
1. 使用 **Tiny** 模型快速测试
2. 验证流程正常后，使用更大的模型重新处理

### 批量处理
1. 对于多个事件，可以先用 **Base** 模型处理
2. 重要的证道使用 **Small** 或 **Medium** 重新生成

### 硬件加速
- 如果有 GPU，whisper.cpp 会自动使用
- 查看服务器日志确认是否启用了 GPU 加速

---

## 下一步功能

可以继续添加：
- [ ] 翻译功能（translate_to_english）
- [ ] 时间戳精度设置
- [ ] 说话人识别（diarization）
- [ ] 自定义词汇表
- [ ] 批量处理多个视频


**现在就试试新功能吧！** 🎉

重启服务器后，在创建事件页面就能看到新的语言和模型选择选项了。

````