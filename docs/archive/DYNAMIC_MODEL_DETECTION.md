# DYNAMIC_MODEL_DETECTION (backup)

Content about `/api/models/whisper` and `/api/models/ollama` endpoints, frontend `getWhisperModels()` and `getOllamaModels()` calls, and model-selection UI were merged into README.md. This backup notes that the details were consolidated; request a full restore if needed.
